#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    string x; cin>>x;
    char f=x[1]; char f2=x[0];
    for(int i=0; i<5; ++i){
        string s; cin>>s;
        if(f==s[1] || f2==s[0]){
            cout<<"YES\n";
            return;
        }
    }
  cout<<"NO\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;
    while (t--) solve();

    return 0;
}