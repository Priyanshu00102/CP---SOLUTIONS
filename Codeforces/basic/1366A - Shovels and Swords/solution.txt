#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define pb push_back
#define F first
#define S second
#define ll long long

using vi = vector<int>;
using pii = pair<int, int>;

const int INF = 1e18;
const int MOD = 1e9 + 7;

// Fast I/O
void fastIO() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}

// Solve one test case
void solve() {
    ll a,b; cin>>a>>b;
       ll cnt=min(min(a,b),(a+b)/3);
    cout<<cnt<<endl;
}

signed main() {
    fastIO();

    int t = 1;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}