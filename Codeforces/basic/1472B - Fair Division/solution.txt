#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    int n; cin>>n;
    int cnt1=0, cnt2=0;
    for(int i=0; i<n; ++i){
        int x; cin>>x;
        if(x==1) cnt1++;
        else cnt2++;
    }
  
  int sum = cnt1 + 2 * cnt2;

if (sum % 2)
    cout << "NO\n";
else if (cnt1 == 0)
    cout << (cnt2 % 2 == 0 ? "YES\n" : "NO\n");
else
    cout << (cnt1 % 2 == 0 ? "YES\n" : "NO\n");
        
    }


int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) solve();

    return 0;
}