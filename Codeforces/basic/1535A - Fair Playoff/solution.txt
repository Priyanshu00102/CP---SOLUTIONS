#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    
    vector<int>v(4);
    for(int &i : v){
        cin>>i;
    }
    if(max(v[0],v[1])<min(v[2],v[3]) || min(v[0],v[1])>max(v[2],v[3])) cout<<"NO\n";
    else cout<<"YES\n"; 

    


    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) solve();

    return 0;
}