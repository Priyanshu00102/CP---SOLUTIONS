#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    int n; cin>>n;
    string s; cin>>s;
    int cnt=0, maxi=0;
    for(int i=0; i<s.size(); i++){
        if(s[i]=='#'){
            cnt++;
          
        }else {
            maxi=max(maxi,cnt);
            cnt=0;
        }
    }
        maxi=max(maxi,cnt);
        
    
    if(maxi%2==0) cout<<maxi/2<<endl;
    else cout<<(maxi/2)+1<<'\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) solve();

    return 0;
}